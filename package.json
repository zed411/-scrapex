{
  "name": "scrapex",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "postinstall": "echo Playwright installed",
    "docker-postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "playwright-core": "^1.60.0"
  }
}
